diff --git a/node_modules/handlebars/lib/index.js b/node_modules/handlebars/lib/index.js
index 455e886..f472a15 100644
--- a/node_modules/handlebars/lib/index.js
+++ b/node_modules/handlebars/lib/index.js
@@ -1,6 +1,3 @@
-import * as fs from 'fs';
-import { createRequire } from 'module';
-
 // IMPORTANT: Don't use `import` with JSON files, otherwise Webpack will fail.
 // See: https://github.com/webpack/webpack/issues/7353#issuecomment-428805998
 
@@ -10,13 +7,7 @@
  */
 
 // ---
-
-const require = createRequire(import.meta.url);
-
-function loadJson(path) {
-  return JSON.parse(fs.readFileSync(require.resolve(path), 'utf8'));
-}
-
+import packageJson from './package.json' assert { type: 'json' };
 import handlebars from './handlebars.js';
 
 // Publish a Node.js require() version of Handlebars
@@ -24,8 +15,7 @@
 
 // Per-release, the version should be updated in the file 'package.json'
 // and synchronized with the version stored in 'defaults.js'.
-const pkg = loadJson('../package.json');
-handlebars.VERSION = pkg.version;
+handlebars.VERSION = packageJson.version;
 
 handlebars.create = function () {
   const hb = handlebars.create();
